{
  "description": "OpenFOAM self-verification hooks. Stop hook verifies that the required OpenFOAM case files exist and pass lightweight dictionary-style structural checks. PostToolUse catches malformed OpenFOAM dictionary writes immediately after Write/Edit/MultiEdit.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/verify_outputs.py",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/verify_openfoam_post_write.py",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
